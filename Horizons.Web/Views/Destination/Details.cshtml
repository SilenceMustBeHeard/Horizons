@using Horizons.Web.ViewModels.Destination
@model DestinationDetailsViewModel

@{
    ViewData["Title"] = Model.Name;
}

<div class="container details-container">
    <div class="row justify-content-center">
        <div class="col-md-10">
            <div class="text-center mb-4">
                <span class="detail-badge">
                    <i class="fas fa-flag-checkered"></i> DESTINATION SPOTLIGHT
                </span>
            </div>
            
            <div class="row">
                <div class="col-md-6">
                    <img src="@(string.IsNullOrEmpty(Model.ImageUrl) ? "/images/default-image.jpg" : Model.ImageUrl)"
                         class="details-image img-fluid rounded shadow-lg"
                         alt="@Model.Name" />
                </div>
                <div class="col-md-6">
                    <h2><i class="fas fa-map-marker-alt" style="color: var(--sport-primary);"></i> @Model.Name</h2>
                    <p class="lead" style="color: var(--sport-text-muted);">@Model.Description</p>
                    
                    <div class="mt-4 p-3" style="background: var(--sport-light); border-radius: 10px;">
                        <p><strong><i class="fas fa-mountain"></i> TERRAIN:</strong> @Model.TerrainName</p>
                        <p><strong><i class="fas fa-calendar-alt"></i> PUBLISHED:</strong> @Model.PublishedOn</p>
                        <p><strong><i class="fas fa-user"></i> ADVENTURER:</strong> @Model.PublisherName</p>
                    </div>

                    <div class="d-flex gap-2 mt-4">
                        <a asp-controller="Destination" asp-action="Index" class="btn btn-secondary">
                            <i class="fas fa-arrow-left"></i> BACK
                        </a>

                        @if (User.Identity.IsAuthenticated && !Model.IsUserPublisher)
                        {
                            <form asp-action="@(Model.IsUserFavourite ? "RemoveFromFavorites" : "AddToFavorites")" method="post" class="d-inline">
                                <input type="hidden" name="id" value="@Model.Id" />
                                <button type="submit" class="btn @(Model.IsUserFavourite ? "btn-danger" : "btn-warning") btn-heart">
                                    @if (Model.IsUserFavourite)
                                    {
                                        <i class="fas fa-heart-broken"></i>
                                       @*  Add to Favorites *@
                                        }
                                    else
                                    {
                                        <i class="fas fa-heart"></i>
                                     @*    Remove from Favorites *@
                                        }
                                </button>
                            </form>
                        }

                        @if (Model.IsUserPublisher)
                        {
                            <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-primary">
                                <i class="fas fa-edit"></i> EDIT
                            </a>
                            <a asp-action="Delete" asp-route-id="@Model.Id" class="btn btn-danger">
                                <i class="fas fa-trash"></i> DELETE
                            </a>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>